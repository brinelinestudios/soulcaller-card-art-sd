build:
  gpu: true
  python_version: "3.10"  # Ensure a supported Python version
  python_packages:
    - torch==2.2.0
    - diffusers==0.25.0
    - safetensors==0.3.3
    - cog==0.8.3
    - requests==2.31.0
    - huggingface_hub==0.19.4  # Add this!

predict: "predict.py:Predictor"